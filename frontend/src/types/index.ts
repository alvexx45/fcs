export interface UserResponse {
  userId: string;
  username: string;
}

export interface ExpenseResponse {
  id: string;
  description: string;
  amount: number;
  date: string;
}

// Add more types as needed
